/**
 * DEPRECATED — do not run. Auth lives on auth.* subdomain, not apex.
 * Use sync-usecoded-domain.mjs instead.
 */
console.error(
  "sync-auth-main-domain.mjs is deprecated. Use: node scripts/sync-usecoded-domain.mjs",
);
process.exit(1);
