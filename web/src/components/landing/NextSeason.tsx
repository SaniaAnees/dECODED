export function NextSeason() {
  return (
    <section id="next" className="scroll-mt-24 mx-auto max-w-5xl px-6 py-8 md:px-8">
      <hr className="rule" />
      <div className="py-24 md:py-32">
        <p className="font-mono text-[11px] tracking-[0.28em] text-gilt">
          NEXT SEASON
        </p>
        <h2 className="mt-5 max-w-2xl font-serif text-4xl font-medium leading-tight text-moon md:text-5xl">
          A coding harness built for token cost, not for spectacle.
        </h2>
        <p className="mt-8 max-w-2xl font-serif text-lg leading-relaxed text-mist md:text-xl">
          The destination is an agentic harness — a coding agent whose job is
          the same work, with less spent on repeated context. We have not
          shipped that agent. We are building toward it, starting from a
          harness rather than inventing one in the abstract.
        </p>
        <p className="mt-6 max-w-2xl font-serif text-lg leading-relaxed text-mist md:text-xl">
          When it exists, this page will say so. Until then, the list is for
          people who care about that problem.
        </p>
      </div>
    </section>
  );
}
